# Project Rules

- When the user says `commit`, treat it as the full GitHub workflow: stage all intended changes, create the commit, and push it to GitHub.
- If the user gives a narrower instruction, follow that narrower scope instead.
